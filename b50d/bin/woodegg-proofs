#!/usr/bin/env ruby
require 'b50d/woodegg'

we = B50D::WoodEgg.new

proofs = we.proofs
if proofs.size > 0
	puts proofs[0]
	id = proofs[0][:id]
	print "Type ok to approve: "
	if 'ok' == gets.strip
		puts we.proof_to_customer(id)
	end
	proofs = we.proofs
else
	puts 'done!'
end

